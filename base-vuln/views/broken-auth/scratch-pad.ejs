<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>scratch-pad</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
        integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
</head>

<body>
    <div class="container">
        <% if (message) { %>
            <div class="alert alert-warning" role="warning">message: <%=message%>
            </div>
            <% } %>
        เว็บไซต์ที่จะให้คุณเขียนเก็บ note ของคุณแบบฟรี ๆ
        <% if (user) { %>
            <p>
                note ของ <%=user.username%>
            </p>
            <form method="post">
                <div class="form-group">
                    <textarea class="form-control" name="note" id="note" cols="30" rows="10"><%=user.note%></textarea>
                </div>
                <button class="btn btn-primary">save note</button>
            </form>
        <% } else { %>
                <p class="my-3">
                    สมัครใช้ note!!
                </p>
                <form action="scratch-pad-login" method="post">
                    <div class="form-group">
                        <input type="text" class="form-control" name="username" id="username" placeholder="username ของคุณ">
                    </div>
                    <div class="form-group">
                        <input type="password" class="form-control" name="password" id="password" placeholder="password ของคุณ">
                    </div>
                    <button class="btn btn-primary">ใช้งาน</button>
                </form>
        <% } %>
    </div>
</body>

</html>