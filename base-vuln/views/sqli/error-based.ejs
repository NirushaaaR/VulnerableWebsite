<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Art Museum</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/3b215afc7a.js" crossorigin="anonymous"></script>
</head>
<body>
    <div class="container">
        <h1 class="text-center">ยินดีต้อนรับสู่พิพิธภัณฑ์ศิลปะของเรา</h1>
        <hr>
        <form method="get">
            <div class="input-group">
                <input type="text" name="search" class="form-control" placeholder="ค้นหางานศิลป์">
                <div class="input-group-append">
                  <button class="btn btn-secondary" type="button">
                    <i class="fa fa-search"></i>
                  </button>
                </div>
              </div>
        </form>

        <% if (err) { %>
            <div class="alert alert-danger my-2" role="alert">
                เกิดข้อผิดพลาด!! <%=err%>
            </div>
        <% } %>

        <div class="row">
            <% if (arts) { %>
            <% arts.forEach(a => {  %>
                <div class="col-md-4">
                    <div class="card mx-4 my-3">
                        <img class="card-img-top" src="<%=a.img%>" style="height: 24rem;">
                        <div class="card-body">
                            <h5 class="card-title"><%=a.title%></h5>
                            <p class="card-text">จิตกร: <%=a.author%></p>
                        </div>
                    </div>
                </div>
            <% }) %>
            <% } %>
            </div>
        </div>
</body>
</html>